[workspace]
members = ["kernel", "user", "mkfs"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true

[profile.release.package.user]
opt-level = "s"
debug = false
strip = true
